TOP := $(dir $(lastword $(MAKEFILE_LIST)))
LIBPATIENT0_INCLUDE=$(TOP)/include
#CFLAGS=-I$(LIBPATIENT0_INCLUDE) -DP0_VERBOSE -DP0_LOG_FILE='"/tmp/p0"' -ggdb3 -g
CFLAGS=-I$(LIBPATIENT0_INCLUDE) -DNDEBUG
